# 重构候选项

TDD 循环完成后，检查以下重构机会：

- **重复逻辑** → 提取函数/类
- **过长方法** → 拆分为私有辅助方法（测试保持在公共接口上）
- **浅模块** → 合并或深化（将复杂性收拢到接口背后）
- **Feature Envy** → 将逻辑移动到数据所在的地方
- **基本类型偏执** → 引入值对象
- **现有代码问题** → 新代码揭示了旧代码的设计缺陷时，一并处理

重构规则：
1. 只在所有测试通过（GREEN）后开始重构
2. 每次重构一小步，每步后运行测试
3. 重构不改变行为，测试不应因重构失败
